<h1>Listing band_data</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Band</th>
      <th>Steps taken</th>
      <th>Calories burnt</th>
      <th>Hearth rate pminute</th>
      <th>Longitude</th>
      <th>Latitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @band_data.each do |band_datum| %>
      <tr>
        <td><%= band_datum.user %></td>
        <td><%= band_datum.band %></td>
        <td><%= band_datum.steps_taken %></td>
        <td><%= band_datum.calories_burnt %></td>
        <td><%= band_datum.hearth_rate_pminute %></td>
        <td><%= band_datum.longitude %></td>
        <td><%= band_datum.latitude %></td>
        <td><%= link_to 'Show', band_datum %></td>
        <td><%= link_to 'Edit', edit_band_datum_path(band_datum) %></td>
        <td><%= link_to 'Destroy', band_datum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Band datum', new_band_datum_path %>
