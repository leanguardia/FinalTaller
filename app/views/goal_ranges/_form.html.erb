<%= form_for(@goal_range) do |f| %>
  <% if @goal_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_range.errors.count, "error") %> prohibited this goal_range from being saved:</h2>

      <ul>
      <% @goal_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :goal_id %><br>
    <%= f.text_field :goal_id %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
